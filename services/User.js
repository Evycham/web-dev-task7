"use strict";

const Tasks = require("./Task");

class User {

    constructor(name, passwd, tasks) {
        this.name = name;
        this.passwd = passwd;
        this.allTasks = [];
    }

    add(task){

    }
}